package es.upsa.a1_prueba1_android_kotlin.pruebas


fun main(){

}

fun testInfix() {

    for( i in 0.until(10) ){

    }

    // Version de until con infix
    for (i in 0 until 10){

    }

    0.untilPer(10)
    0 untilPerInfix 10

    // El infix aparte de los until se puede utilizar en los mapas
    val map = mapOf(Pair("a", 1), Pair("b", 2))

    val MapInfix = mapOf( "a" to 1, "b" to 2 )
}

// Funcion sin Infix
fun Int.untilPer(x:Int){

}

// Funcion con Infix, añade infix diferencia al llamarla
infix fun Int.untilPerInfix(x:Int) {

}
